<div class="page-width">
  <div class="empty-state">
    <span class="eyebrow">404</span>
    <h1>{{ 'page_404.title' | t }}</h1>
    <p class="text-muted">{{ 'page_404.body' | t }}</p>
    <a class="button" href="{{ routes.all_products_collection_url }}">{{ 'page_404.cta' | t }}</a>
  </div>
</div>

{% schema %}
{
  "name": "404 page"
}
{% endschema %}
